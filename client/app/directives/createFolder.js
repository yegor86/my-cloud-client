(function (angular) {
    "use strict";

    var module = angular.module('myCloudDriveApp');

    module.directive('createFolder', function ($document, $state) {

        return {
            link: function ($scope, $element) {

            }
        };
    });
}(angular));
